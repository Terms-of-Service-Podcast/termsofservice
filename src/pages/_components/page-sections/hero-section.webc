<div id="hero-section" class="page-section">
  <div id="hero-section-bg"></div>
  <div id="hero-section-fg">
    <div id="hero-section-text-container">
      <div id="hero-section-text">
        <h2 class="no-line">Welcome</h2>
        <h3>Thanks for jumping into conversation</h3>
        <p>
          We’re going to dive deep into the fine print of our digital lives. 
          We’ll chat with the people who write the code, the contracts and 
          the policies that effect our experiences online.
        </p>
          Our hope is that these conversations make sense to everyone who is 
          a user of the Internet. Our goal is that you feel more informed about 
          the choices you make when you interact online with tech, with brands, 
          and with each other. 
        <p>
      </div>
    </div>
  </div>
  <div id="hero-section-hero-image-container">
    <svg id="hero-section-hero-image-bg" viewBox="0 0 300 300" 
      preserveAspectRatio="xMinYMin"
      xmlns="http://www.w3.org/2000/svg" fill="#fff">
      <path d="M 0 200 A 20 21 0 1 1 200 200 L 200 300 L 0 300 Z" />
    </svg>
    
    <img id="hero-section-hero-image" src="/img/mary-stairs-transparent.png" alt="Mary Camacho">
  </div>
</div>

<style>
  #hero-section.page-section {
    padding: 0;
    position: relative;
    overflow: hidden;
    max-height: 520px;
  }

  #hero-section-bg {
    position: absolute;
    left: 0;
    width: 100%;
    /* messing with the top and height to get the blurry edges to not show */
    top: -15px;
    height: 105%;
    background-image: url('/img/tos-water-bg.png');
    background-size: cover;
    background-position: center;
    filter: blur(var(--bg-image-blur-value));
    overflow: hidden;
  }

  #hero-section-fg {
    margin: 100px 0;
    position: relative;
    z-index: 1;
    text-align: center;
    background-color: var(--color-blue-1);
  }

  #hero-section-text-container {
    max-width: var(--spacing-max-content-width);
    margin: 0 auto;

    #hero-section-text {
      text-align: left;
      max-width: 50%;
      color: var(--color-white);
      padding: 1em 0;
    

      h2 {
        color: var(--color-white);
      }
    }
  }

  #hero-section-hero-image-container {
    border: 1px solid red;
    display: grid;
    grid-template-columns: [cols-start] 1fr [cols-end]; 
    grid-template-rows: [rows-start] auto [rows-end];
    position: absolute;
    top: 0;
    right: 0;
    width: 30%;
    height: 100%;
    max-height: 520px;
    z-index: 1;
    overflow: hidden;

    #hero-section-hero-image-bg {
      grid-area: rows-start / cols-start / rows-end / cols-end;
      display: block;
      /* position: absolute; */
      /* bottom: 0; */
      /* right: 0; */
      /* top: 0; */
      /* left: 0; */
      /* height: 100%; */
      width: 500px;
      height: 500px;

      /* background-color: var(--color-white); */
    }

    #hero-section-hero-image {
      grid-area: rows-start / cols-start / rows-end / cols-end;
      visibility: hidden;
      /* position: absolute; */
      /* position: relative; */
      /* bottom: 0; */
      /* right: 60px; */
      /* text-align: right; */
      /* width: 100%; */
      max-height: 500px;
    }
  }
  
</style>